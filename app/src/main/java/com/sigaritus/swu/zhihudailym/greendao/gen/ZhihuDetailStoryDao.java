package com.sigaritus.swu.zhihudailym.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.sigaritus.swu.zhihudailym.bean.ZhihuDetailStory;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ZHIHU_DETAIL_STORY".
*/
public class ZhihuDetailStoryDao extends AbstractDao<ZhihuDetailStory, String> {

    public static final String TABLENAME = "ZHIHU_DETAIL_STORY";

    /**
     * Properties of entity ZhihuDetailStory.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, String.class, "id", true, "ID");
        public final static Property Body = new Property(1, String.class, "body", false, "BODY");
        public final static Property Image_source = new Property(2, String.class, "image_source", false, "IMAGE_SOURCE");
        public final static Property Title = new Property(3, String.class, "title", false, "TITLE");
        public final static Property Image = new Property(4, String.class, "image", false, "IMAGE");
        public final static Property Share_url = new Property(5, String.class, "share_url", false, "SHARE_URL");
    };


    public ZhihuDetailStoryDao(DaoConfig config) {
        super(config);
    }
    
    public ZhihuDetailStoryDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ZHIHU_DETAIL_STORY\" (" + //
                "\"ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: id
                "\"BODY\" TEXT," + // 1: body
                "\"IMAGE_SOURCE\" TEXT," + // 2: image_source
                "\"TITLE\" TEXT," + // 3: title
                "\"IMAGE\" TEXT," + // 4: image
                "\"SHARE_URL\" TEXT);"); // 5: share_url
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ZHIHU_DETAIL_STORY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ZhihuDetailStory entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
 
        String body = entity.getBody();
        if (body != null) {
            stmt.bindString(2, body);
        }
 
        String image_source = entity.getImage_source();
        if (image_source != null) {
            stmt.bindString(3, image_source);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(4, title);
        }
 
        String image = entity.getImage();
        if (image != null) {
            stmt.bindString(5, image);
        }
 
        String share_url = entity.getShare_url();
        if (share_url != null) {
            stmt.bindString(6, share_url);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ZhihuDetailStory entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
 
        String body = entity.getBody();
        if (body != null) {
            stmt.bindString(2, body);
        }
 
        String image_source = entity.getImage_source();
        if (image_source != null) {
            stmt.bindString(3, image_source);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(4, title);
        }
 
        String image = entity.getImage();
        if (image != null) {
            stmt.bindString(5, image);
        }
 
        String share_url = entity.getShare_url();
        if (share_url != null) {
            stmt.bindString(6, share_url);
        }
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public ZhihuDetailStory readEntity(Cursor cursor, int offset) {
        ZhihuDetailStory entity = new ZhihuDetailStory( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // body
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // image_source
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // title
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // image
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5) // share_url
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ZhihuDetailStory entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setBody(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setImage_source(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setTitle(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setImage(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setShare_url(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
     }
    
    @Override
    protected final String updateKeyAfterInsert(ZhihuDetailStory entity, long rowId) {
        return entity.getId();
    }
    
    @Override
    public String getKey(ZhihuDetailStory entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
